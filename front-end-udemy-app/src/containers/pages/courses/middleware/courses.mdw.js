export const handleCoursePage = {
  checkTypePage: (url, params, querys) => {},
  loadCourseByParams: (params = {}) => {
    switch (Object.keys(params).length) {
      case 0:
        break;
      default:
        break;
    }
  },
  loadCourseBySearchText: (searchText = "") => {},
};
